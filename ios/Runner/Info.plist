<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Uygulama performansı için optimizasyon -->
    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>

    <!-- Uygulama geliştirme bölgesi -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <!-- Uygulama görünen adı -->
    <key>CFBundleDisplayName</key>
    <string>Servis App</string>

    <!-- Uygulama çalıştırılabilir dosya adı -->
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <!-- Uygulama paket kimliği -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <!-- Info.plist sürümü -->
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- Uygulama adı -->
    <key>CFBundleName</key>
    <string>servis_app</string>

    <!-- Uygulama paket türü -->
    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <!-- Uygulama kısa versiyon numarası -->
    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>

    <!-- Uygulama imzası -->
    <key>CFBundleSignature</key>
    <string>????</string>

    <!-- Uygulama versiyon numarası -->
    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <!-- iOS gereksinimi -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Dolaylı giriş olayları desteği -->
    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

    <!-- Başlangıç ekranı -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <!-- Ana storyboard dosyası -->
    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <!-- Desteklenen ekran yönleri -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- iPad için desteklenen ekran yönleri -->
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Kamera izni açıklaması -->
    <key>NSCameraUsageDescription</key>
    <string>QR kod taramak için kameraya erişim gereklidir.</string>

    <!-- Mikrofon izni (eğer gerekiyorsa) -->
    <!-- <key>NSMicrophoneUsageDescription</key>
    <string>Ses kaydı yapmak için mikrofona erişim gereklidir.</string> -->

    <!-- Konum izni (eğer gerekiyorsa) -->
    <!-- <key>NSLocationWhenInUseUsageDescription</key>
    <string>Konum bilgisi için izin gereklidir.</string> -->
</dict>
</plist>